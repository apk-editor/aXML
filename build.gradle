buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath libs.gradle
    }
}

tasks.register('clean', Delete) {
    delete rootProject.buildDir
}